import { ComponentFixture, TestBed } from '@angular/core/testing';
import { AtencionespsicologoPage } from './atencionespsicologo.page';

describe('AtencionespsicologoPage', () => {
  let component: AtencionespsicologoPage;
  let fixture: ComponentFixture<AtencionespsicologoPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(AtencionespsicologoPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
