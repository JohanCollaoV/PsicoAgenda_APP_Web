import { ComponentFixture, TestBed } from '@angular/core/testing';
import { SoportepsicologoPage } from './soportepsicologo.page';

describe('SoportepsicologoPage', () => {
  let component: SoportepsicologoPage;
  let fixture: ComponentFixture<SoportepsicologoPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(SoportepsicologoPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
