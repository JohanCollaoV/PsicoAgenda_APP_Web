import { ComponentFixture, TestBed } from '@angular/core/testing';
import { MantenedorusuariosPage } from './mantenedorusuarios.page';

describe('MantenedorusuariosPage', () => {
  let component: MantenedorusuariosPage;
  let fixture: ComponentFixture<MantenedorusuariosPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(MantenedorusuariosPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
