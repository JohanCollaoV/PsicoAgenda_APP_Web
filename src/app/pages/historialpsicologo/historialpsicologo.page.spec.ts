import { ComponentFixture, TestBed } from '@angular/core/testing';
import { HistorialpsicologoPage } from './historialpsicologo.page';

describe('HistorialpsicologoPage', () => {
  let component: HistorialpsicologoPage;
  let fixture: ComponentFixture<HistorialpsicologoPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(HistorialpsicologoPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
